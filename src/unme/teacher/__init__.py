"""Teacher client + trace generation.

GLM Task 2 populates `client.py` (httpx → OpenAI-compatible serving endpoint, top-k
logprobs) and `generate.py` (emits `unme.schemas.Trace` JSONL). Left intentionally
empty so importing the package does not pull unfinished modules.
"""
