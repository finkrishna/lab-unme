"""Eval harness + regression gate.

GLM Task 3 populates `harness.py` with `evaluate(...) -> unme.schemas.GateReport`.
Left intentionally empty so importing the package does not pull unfinished modules.
"""
